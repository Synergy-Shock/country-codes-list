name: Publish to NPM

on:
  push:
    tags:
      - "v*"

# Required for Trusted Publishing (OIDC) — no NPM_TOKEN needed.
permissions:
  id-token: write
  contents: read

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: npm
      # Node 20 ships npm 10; Trusted Publishing requires npm >= 11.5.1.
      # Pin to the 11.x line: npm@latest is now 12.x, which drops Node 20.
      - name: Upgrade npm for Trusted Publishing
        run: npm install -g npm@11
      - name: Install dependencies
        run: npm ci
      - name: Build project
        run: npm run build
      - name: Run tests
        run: npm test
      # Auth is via OIDC; provenance is generated automatically on public repos.
      - name: Publish package
        run: npm publish
